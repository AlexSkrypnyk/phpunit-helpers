name: Test PHP

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - 'feature/**'
  workflow_dispatch:
    inputs:
      enable_terminal:
        type: boolean
        description: 'Enable terminal session.'
        required: false
        default: false

permissions:
  contents: read

jobs:
  test-php:
    name: PHP ${{ matrix.php-versions }}, Symfony ${{ matrix.symfony }}
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write # Post the coverage summary as a PR comment.

    strategy:
      fail-fast: false
      matrix:
        php-versions: ['8.3', '8.4', '8.5']
        symfony: ['6.4 lowest', '6.4 highest', '7.2 lowest', '7 highest', '8.0 lowest', '8 highest']
        exclude:
          # Symfony 8 requires PHP 8.4.
          - php-versions: '8.3'
            symfony: '8.0 lowest'
          - php-versions: '8.3'
            symfony: '8 highest'
        include:
          - symfony: '6.4 lowest'
            major: '6'
            bound: 'lowest'
            # The floor of the whole tree, which also covers PHPUnit 11.4.
            flags: '--prefer-lowest --prefer-stable'
          - symfony: '6.4 highest'
            major: '6'
            bound: 'highest'
            flags: ''
          - symfony: '7.2 lowest'
            major: '7'
            bound: 'lowest'
            flags: ''
          - symfony: '7 highest'
            major: '7'
            bound: 'highest'
            flags: ''
          - symfony: '8.0 lowest'
            major: '8'
            bound: 'lowest'
            flags: ''
          - symfony: '8 highest'
            major: '8'
            bound: 'highest'
            flags: ''

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Cache Composer dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: /tmp/composer-cache
          key: ${{ runner.os }}-${{ matrix.symfony }}-${{ hashFiles('**/composer.lock') }}

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: ${{ matrix.php-versions }}
          coverage: pcov
          ini-values: pcov.directory=.

      - name: Resolve the Symfony version from composer.json
        run: php .github/scripts/symfony-constraint.php --major=${{ matrix.major }} --bound=${{ matrix.bound }} >> "$GITHUB_ENV"

      - name: Install dependencies
        run: composer update --with "symfony/console:${SYMFONY_VERSION}" --with "symfony/filesystem:${SYMFONY_VERSION}" --with "symfony/finder:${SYMFONY_VERSION}" --with "symfony/process:${SYMFONY_VERSION}" ${{ matrix.flags }} ${{ matrix.php-versions == '8.5' && '--ignore-platform-reqs' || '' }}

      - name: Validate composer.json
        run: |
          composer --verbose validate
          composer normalize --dry-run

      - name: Check the installed Symfony version
        run: |
          for package in symfony/console symfony/filesystem symfony/finder symfony/process; do
            version=$(composer show "${package}" --format=json | jq -r '.versions[0]')
            echo "${package} ${version}"
            if [[ "${version}" != "${SYMFONY_EXPECTED}"* ]]; then
              echo "${package} ${version} is not ${SYMFONY_EXPECTED} for this leg."
              exit 1
            fi
          done

      - name: Check coding standards
        if: matrix.php-versions == '8.4' && matrix.symfony == '8 highest'
        run: composer lint
        continue-on-error: ${{ vars.CI_LINT_IGNORE_FAILURE == '1' }}

      - name: Run tests
        run: composer test-coverage
        continue-on-error: ${{ vars.CI_TEST_IGNORE_FAILURE == '1' }}

      - name: Check code coverage threshold
        if: matrix.php-versions == '8.4' && matrix.symfony == '8 highest'
        run: |
          RATE=$(grep -o 'line-rate="[0-9.]*"' .logs/cobertura.xml | head -1 | tr -cd '0-9.')
          PERCENT=$(awk "BEGIN {printf \"%.2f\", $RATE*100}")
          echo "Coverage: $PERCENT% (threshold: $CI_CODE_COVERAGE_THRESHOLD%)"
          if [[ ${PERCENT//./} -lt $((CI_CODE_COVERAGE_THRESHOLD*100)) ]]; then
            echo "FAIL: coverage too low"
            exit 1
          fi
          { echo "COVERAGE_CONTENT<<EOF"; sed '/./,$!d' .logs/coverage.txt; echo "EOF"; } >> "$GITHUB_ENV"
        env:
          CI_CODE_COVERAGE_THRESHOLD: ${{ vars.CI_CODE_COVERAGE_THRESHOLD || '80' }}

      - name: Post coverage summary as PR comment
        if: matrix.php-versions == '8.4' && matrix.symfony == '8 highest' && github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@70d2764d1a7d5d9560b100cbea0077fc8f633987 # v3
        with:
          message: |
            ```
            ${{ env.COVERAGE_CONTENT }}
            ```
          # Post a new comment every time to keep the coverage history.
          hide_and_recreate: true

      - name: Upload coverage report as an artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{github.job}}-code-coverage-report-${{ matrix.php-versions }}-${{ matrix.symfony }}
          path: .logs
          include-hidden-files: true
          if-no-files-found: error

      - name: Upload test results to Codecov
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7
        if: ${{ !cancelled() && matrix.php-versions == '8.4' && matrix.symfony == '8 highest' && env.CODECOV_TOKEN != '' }}
        with:
          files: .logs/junit.xml
          report_type: test_results
          fail_ci_if_error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7
        if: matrix.php-versions == '8.4' && matrix.symfony == '8 highest' && env.CODECOV_TOKEN != ''
        with:
          files: .logs/cobertura.xml
          fail_ci_if_error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Setup tmate session
        if: ${{ !cancelled() && github.event.inputs.enable_terminal }}
        uses: mxschmitt/action-tmate@35b54afac29c97fb54faba5b513f8fbd1882f113 # v3
        timeout-minutes: 30
